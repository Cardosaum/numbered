[workspace]
repo_url = "https://github.com/Cardosaum/numbered"
pr_labels = ["release"]
# First GitHub release: create tags/releases on this main push. crates.io
# already has 0.1.0, so cargo publish is skipped. Flip back to false after.
release_always = true

[[package]]
name = "numbered"
version_group = "numbered"
changelog_include = ["numbered-macros"]
changelog_path = "CHANGELOG.md"

[[package]]
name = "numbered-macros"
version_group = "numbered"
changelog_path = "macros/CHANGELOG.md"
# cargo-semver-checks does not apply to proc-macro crates.
semver_check = false
